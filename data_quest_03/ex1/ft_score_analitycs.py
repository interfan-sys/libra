#!/usr/bin/env/ python3

import sys

def main():
    print("=== Player Score Analytics ===")
    arg_len = len(sys.argv)
    i = 1
    while i < arg_len:
        try:
            score: int = int(sys.argv[i])

    valid_scores += score
    while arg_len:
        print(f"Total players: {arg_len}")
        print(f"Scores processed: {mylist}")
        print(f"Total score: {sum(mylist)}")


if __name__ == "__main__":
    main()